{
  "name": "prepa-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/verifier-accord-page.mjs && vitest run",
    "test:watch": "vitest",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply prepa --local",
    "db:prod": "wrangler d1 migrations apply prepa --remote",
    "test:accord": "node scripts/verifier-accord-page.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "^2.0.5",
    "wrangler": "^3.78.0"
  }
}
